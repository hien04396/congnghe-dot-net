@model OnlineShop.Models.ProductPromo

@{
    ViewData["Title"] = "Edit Promo";
    var products = (IEnumerable<OnlineShop.Models.Product>)ViewData["Products"]!;
    var productId = Context.Request.Query["productId"].ToString();
}

<h2>Edit Promo</h2>

<form asp-action="Edit" method="post">
    <input type="hidden" asp-for="Id" />
    @if (!string.IsNullOrEmpty(productId))
    {
        <input type="hidden" name="returnProductId" value="@productId" />
    }
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <div class="mb-3">
        <label class="form-label">Product</label>
        <select asp-for="ProductId" class="form-select">
            @foreach (var p in products)
            {
                <option value="@p.Id" selected="@(p.Id == Model.ProductId ? "selected" : null)">@p.Name</option>
            }
        </select>
        <span asp-validation-for="ProductId" class="text-danger"></span>
    </div>
    <div class="mb-3">
        <label asp-for="AmountOff" class="form-label"></label>
        <input asp-for="AmountOff" class="form-control" />
        <span asp-validation-for="AmountOff" class="text-danger"></span>
    </div>
    <div class="mb-3">
        <label asp-for="StartDate" class="form-label"></label>
        <input asp-for="StartDate" class="form-control" />
        <span asp-validation-for="StartDate" class="text-danger"></span>
    </div>
    <div class="mb-3">
        <label asp-for="EndDate" class="form-label"></label>
        <input asp-for="EndDate" class="form-control" />
        <span asp-validation-for="EndDate" class="text-danger"></span>
    </div>
    <div class="form-check mb-3">
        <input asp-for="IsActive" class="form-check-input" />
        <label asp-for="IsActive" class="form-check-label"></label>
    </div>
    <button type="submit" class="btn btn-primary">Save</button>
    <a asp-action="Index" asp-route-productId="@productId" class="btn btn-secondary">Cancel</a>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}


